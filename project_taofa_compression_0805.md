---
name: project_taofa_compression_0805
description: "115.08.05已完成｜打法說明壓縮成「手冊」格式(只留現在該怎麼做,不含敘事/日期/原話)；141A/141E請款單+修改單四份全部壓縮完成，四支回測全綠，並新增PreToolUse hook技術強制擋裸.Save()"
metadata: 
  node_type: memory
  type: project
  modified: 2026-08-06T00:36:18.689Z
  originSessionId: 0974a490-0c50-4d7e-8db9-69d12eb0bb2e
---

使用者115.08.05要求：打法說明太長(146+147條，日記體含敘事/日期/原話)，每次動手前都要讀，200字跟2000字消耗的token不一樣、也更容易在敘事裡讀錯現行規則。使用者明確定調：只要「手冊」(現行規則、精簡、快速達成)，不要保留第二層「沿革記錄」給我平常讀——但我仍保留了原始內容做封存備份(不是使用者要的東西，是我自己為了不刪除稽核紀錄留的安全網)。

**Why**：打法說明是每次要重新讀一遍自己判斷怎麼套用的文字，不是寫了就自動生效的程式；字數增加＝每次讀取token線性增加，但正確套用率不會跟著字數變好，甚至可能因為敘事太多讀錯（今天平安#15行通表memo那次錯就是在A213一大段敘事裡把自己延伸發揮的話跟使用者原話混在一起）。

## 已完成：141A
- 原文57KB/449列(A1~A217) → 精簡版111列/約8.5KB，壓縮到約15%。
- 做法：原分頁「請款單打法說明」改名封存為「打法說明_沿革封存115.08.05」(保留在同一活頁簿，不刪除)；新建同名「請款單打法說明」分頁放精簡版，每條=現在該怎麼做，不含使用者原話引用(除非該原話本身就是要逐字輸出的memo模板)、不含日期、不含案例背景。
- 新增驗證腳本`Downloads\agent\計價回測工具\_精簡版覆蓋率回測.ps1`：用精簡版自己的字眼當關鍵字，驗證壓縮後有沒有漏掉過去定案的規則概念(不是驗舊敘事字串還在不在，那支是`_規則同步回測.ps1`的T2，兩者用途不同)。首跑69項全PASS(先抓到1個測項自己的標點打錯導致假FAIL，修正後全過)。
- 過程中意外發現並清理：①另一個並行動作已在A216定案「標色制度廢止」(115.08.05)，我今天稍早幫呂發#7標的橘色tab已是過時動作；②A191已定案「總表特別說明欄取消」(115.08.03，在本次壓縮任務之前就已存在)，但我今天處理呂發#7/翔博#4/勝鈞#2等案時誤把說明文字寫進這個應該留空的分隔欄，已清空修正(內容仍完整保留在查核記錄.txt與M欄，只是搬離了不該放的欄位)。
- 順便修好`_計價本回測.ps1`的`$skipSheets`漏了新增的封存分頁名稱，導致把封存分頁誤判成請款案子分頁報5項假FAIL，已修正。

## 已完成：141E 請款單打法說明（115.08.05 同一輪接續完成）
- 141E原文57KB/451列，內容跟141A約99%重複（同一套公司流程），只有混凝土抽磅memo六段格式的具體範例（141E-PO003866台灣水泥#1/#2）是141E原創、141A後續跟進同格式。
- 壓縮做法：直接沿用141A精簡版內容，加註混凝土範例出處、案場路徑改141E北士科軟橋段，其餘規則完全共用。
- 141E總表活頁簿實測已是新版待核/核定兩區格式（比原文紀錄A198寫的「141E尚未跟進」更新，是另一個並行動作已經做的）。
- `_精簡版覆蓋率回測.ps1`已擴充成同時驗141A+141E兩本，68項×2=136項全PASS。

## 已完成：141A/141E 修改單打法說明（同一輪）
- 兩本修改單打法說明內容幾乎一致（僅141E多一條COST+FEE變體範例），原文各約3.8~4KB，壓縮成一份共用精簡版(29行)寫進兩本，內含：7格公式化範本、核決層級四張表、第一行括弧核決分類寫法(絕不寫工項名)、業變經理打法七步驟(含COST+FEE變體)、命名/總表流水號規則、Downloads整理夾流程。
- 原分頁都改名封存為「修改單打法說明_沿革封存115.08.05」，同活頁簿保留。

## 收尾動作
- `_規則同步回測.ps1`的T2關鍵字表已從90條舊敘事關鍵字換成32條精簡版核心關鍵字；同時修正一個門檻bug：COM讀取健全性自檢原本寫「讀到字數<10000視為COM讀壞」，這個門檻是照舊版57KB內容校準的，精簡版正常只有8000~9000字，會被誤判成讀取失敗而中止，已調降成3000（跟`_精簡版覆蓋率回測.ps1`一致）。
- 最終全套回測：計價本回測13/13、追蹰報表回測78/78、規則同步回測117/117、精簡版覆蓋率回測136/136，四支全綠。
- 新增PreToolUse hook `.claude\hooks\excel-save-guard.ps1`（掛在settings.json），技術性擋下裸`.Save()`與非暫存路徑的`SaveAs()`，已現場實測真的會擋下工具呼叫——這是回應使用者「要怎麼強制不要再犯這個錯」的解法，把記憶提醒升級成工具層強制。

四份核心打法說明（141A/141E的請款單+修改單）全部壓縮完成，都採用「現行規則(精簡)+沿革封存(同活頁簿archive分頁,不刪除)」的兩層架構。

相關：[[feedback_backtest_discipline]]、[[feedback_user_edit_implies_rule_0730]]、[[feedback_desktop_excel_inplace_save]]
