---
name: project_taofa_compression_0805
description: "115.08.05進行中｜打法說明壓縮成「手冊」格式(只留現在該怎麼做,不含敘事/日期/原話)；141A已完成，141E尚未開始"
metadata: 
  node_type: memory
  type: project
  modified: 2026-08-05T08:57:42.918Z
  originSessionId: 0974a490-0c50-4d7e-8db9-69d12eb0bb2e
---

使用者115.08.05要求：打法說明太長(146+147條，日記體含敘事/日期/原話)，每次動手前都要讀，200字跟2000字消耗的token不一樣、也更容易在敘事裡讀錯現行規則。使用者明確定調：只要「手冊」(現行規則、精簡、快速達成)，不要保留第二層「沿革記錄」給我平常讀——但我仍保留了原始內容做封存備份(不是使用者要的東西，是我自己為了不刪除稽核紀錄留的安全網)。

**Why**：打法說明是每次要重新讀一遍自己判斷怎麼套用的文字，不是寫了就自動生效的程式；字數增加＝每次讀取token線性增加，但正確套用率不會跟著字數變好，甚至可能因為敘事太多讀錯（今天平安#15行通表memo那次錯就是在A213一大段敘事裡把自己延伸發揮的話跟使用者原話混在一起）。

## 已完成：141A
- 原文57KB/449列(A1~A217) → 精簡版111列/約8.5KB，壓縮到約15%。
- 做法：原分頁「請款單打法說明」改名封存為「打法說明_沿革封存115.08.05」(保留在同一活頁簿，不刪除)；新建同名「請款單打法說明」分頁放精簡版，每條=現在該怎麼做，不含使用者原話引用(除非該原話本身就是要逐字輸出的memo模板)、不含日期、不含案例背景。
- 新增驗證腳本`Downloads\agent\計價回測工具\_精簡版覆蓋率回測.ps1`：用精簡版自己的字眼當關鍵字，驗證壓縮後有沒有漏掉過去定案的規則概念(不是驗舊敘事字串還在不在，那支是`_規則同步回測.ps1`的T2，兩者用途不同)。首跑69項全PASS(先抓到1個測項自己的標點打錯導致假FAIL，修正後全過)。
- 過程中意外發現並清理：①另一個並行動作已在A216定案「標色制度廢止」(115.08.05)，我今天稍早幫呂發#7標的橘色tab已是過時動作；②A191已定案「總表特別說明欄取消」(115.08.03，在本次壓縮任務之前就已存在)，但我今天處理呂發#7/翔博#4/勝鈞#2等案時誤把說明文字寫進這個應該留空的分隔欄，已清空修正(內容仍完整保留在查核記錄.txt與M欄，只是搬離了不該放的欄位)。
- 順便修好`_計價本回測.ps1`的`$skipSheets`漏了新增的封存分頁名稱，導致把封存分頁誤判成請款案子分頁報5項假FAIL，已修正。

## 尚未開始：141E
- 141E打法說明約147條，完整內容還沒讀過(只讀過141A)。
- `_規則同步回測.ps1`的T1(兩本條文數差距)、T2(兩本都要有同一批關鍵字)現在會大量FAIL，因為141A已經是精簡版新字眼、141E還是舊敘事版——這是預期中的過渡態，不是新錯誤，等141E也壓縮完、且把T2關鍵字表改成對照精簡版字眼後才會重新一致。
- 下次要做：①完整讀141E原文②比照141A的分類架構壓縮(注意141E可能有141A沒有的專屬規則，如混凝土材料memo六段格式範本、連續壁抽磅等，141A精簡版裡引用了"141E對應分頁"要確認引用還有效)③141E也建archive分頁④跑對應覆蓋率回測⑤修`_規則同步回測.ps1`的T2清單改成兩本都用精簡版字眼比對。

相關：[[feedback_backtest_discipline]]、[[feedback_user_edit_implies_rule_0730]]
