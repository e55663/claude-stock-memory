---
name: feedback_session_cost_and_memory_slimming
description: 額度真正花在哪+記憶瘦身原則;每檔clear有開場稅別每檔清、規則層vs案件層分流、封存vs刪額度差≈0
metadata: 
  node_type: memory
  type: feedback
  originSessionId: aa6a4d5b-2223-43f9-aeca-85bdf9edbc61
---

**(2026/06/23 共同釐清額度與記憶管理)** 使用者原以為「每個檔做完就 /clear、重開新對話」最省額度，這是錯的，已導正。

🔴 核心觀念：**決定額度的是「每次開場載入的索引大小」＋「做事當下讀了多少檔」，不是硬碟躺幾個舊檔。**
- `/clear` 不退額度，只清 context。每次 /clear＝重開 session＝**重付一次固定開場稅**（CLAUDE.md＋約 35KB 的 MEMORY.md 索引＋骨架，估約 1.5 萬 tokens）。每檔都清＝把這筆重稅付十幾遍／天，還每檔重讀檔＝雙重浪費。
- 一個舊案檔只要(1)不在 MEMORY.md 索引、(2)沒被我讀到 → 對額度完全隱形。

🔴 **新工作節奏（取代「每檔 clear」）**：處理一批案件 → **告一段落才 `收尾`＋`/clear`**，不要每單檔清。獨立案之間仍要清（避免 A 案數字混進 B 案核對＝踩「絕不掰」），但「告一段落」抓在「一個案群處理完／對話開始拖變慢」時，別硬撐十幾個不相干案塞同一視窗（context 越疊越貴＋交叉污染風險）。延伸自 [[reference_session_workflow_habit]]。

🔴 **規則層 vs 案件層分流**（使用者最在意：只要規則記好）：
- 規則層＝過程中冒出的新想法／新規矩／長官要求改的（reference_／feedback_）→ 進索引、記紮實、覆蓋舊的不堆疊。數量會「收斂」（規則就那些，記滿幾乎不再長）。**這是資產，唯一要顧好的。**
- 案件層＝某案某期處理流水／數字（project_*）→ **不佔索引**；Excel 裡文字都建好了、要用再丟檔給我重分析，所以這類半廢。需要保留就封存，不必每案另開索引行。
- 目標：讓開場成本只跟「規則數」長（會停），不跟「案件數」長（無限）。

🔴 **封存 vs 刪 額度差 ≈ 0**：兩者每次開場成本都趨近零（差別只在硬碟空間，不花額度）。所以舊案 **封存不刪**＝同成本還留保險，沒理由為省額度刪檔。

**Why:** 使用者每天工作約 10h、上下午常把 session 額度用滿；他的工地請款是「丟檔→照規則做→歸檔→不回頭」的一次性場景，不像選股會回頭，所以案件快照對他幾乎無未來 recall 價值，真正值錢的是規則與長官要求。
**How to apply:** 我 `收尾` 時把重點往規則層抓、案件流水不灌進索引；不主動刪舊案（封存即可）；提醒使用者別每檔 clear、改成段落式收尾。

**使用者否決**：不要新增 `/日結`、不要「輕量收尾」拆分——嫌多流程更複雜。`收尾` 維持原樣（到段落總結：犯的錯＋新規則→確認記憶吃到），[[reference_session_workflow_habit]] 的 收尾→clear 流程不變。

**落地狀態（2026/06/23）**：
- ✅ 已刪重複的專案層 `C:\Users\Seal_Lo\Downloads\agent\CLAUDE.md`（與家目錄層 `C:\Users\Seal_Lo\CLAUDE.md` 逐位元組相同，3,202 字），保留家目錄層→每 session 開場少載約 3,200 字。
- ✅（6/23 完成）MEMORY.md 索引已從整段式瘦成「一行鉤子」：35,343→18,924 bytes（砍約46%、每 session 少載約16KB）；72 條全保留、0 條掉、連結全對應（git 逐條比對驗過）。原則=每條只留連結+觸發關鍵字+🔴關鍵雷/載重數字，細節留各內頁檔；驗證後證實改前改後對同一案(銲昇#1)的意見完全一致。
- 🟠 順手抓到設定不一致：`settings.local.json` 記憶 Edit/Write 白名單指向 `…\projects\C--Users-Seal-Lo\memory`，但實際專案路徑是 `C--Users-Seal-Lo-Downloads-agent\memory`，對不上→在本專案寫記憶會多跳權限框（待修白名單）。
