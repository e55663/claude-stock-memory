---
name: reference_archive_workflow_0806
description: 歸檔流程手冊(整合6檔):先staging開整理夾/命名與工地位置/問題件夾/進件查重刪舊留新/歸檔指令語意/歸檔後刪該案記憶;含回測測項
metadata: 
  node_type: memory
  type: reference
  originSessionId: 365bbf1a-11d3-4cf1-8927-79cb45fcc87f
  modified: 2026-08-12T08:49:36.073Z
---

# 歸檔流程手冊

## 〇、🔴🔴🔴 Downloads 路徑地圖（115.08.12 使用者重整，舊路徑全失效）
- 根目錄只剩 5 項：`1.公司`／`2.工地`／`3.請款單 修改單`／`agent`／`個人`。動任何檔案前先看這條。
- 改名：`公司`→**`1.公司`**、`工地`→**`2.工地`**。歸檔目的地一律 `Downloads\2.工地\2.141A 嘉科AP7P1-OFFICE\2.請款單\115.MM\`。
- 五個母夾（`0.5 採購議價`／`1.送出待簽核完成歸檔`／`2.退件`／`3.明天整理`／`4.核准`）**全部收進 `3.請款單 修改單`**，不再掛根目錄。
- 🔴 **活件＝`Downloads\3.請款單 修改單` 直屬**（等同舊制的 Downloads 根目錄）。根目錄**不再放任何案夾**；判「還在我手上」只看這裡。
- 🔴 **投入計價歸檔兩案場慣例不同，別統一**：141E＝`6.投入及計價\115.MM\` 月份夾；141A＝`預定收入投入 AP7P1 OFFICE\` 平鋪＋檔名帶案場後綴。141A 底下的 `6.投入及計價` 是空的、**別用**。
- 🔴 **成控月報的檔名後綴＝案場工作版，不是重複檔**：無後綴＝公司原始全工地版（內含 121E/131J/141A/141C/141E 全部分頁）；`北士科`＝141E 版；`P1 OFFICE`/`AP7P1 OFFICE`/`- p1 office`＝141A 版。同月不同後綴要**分開歸各自案場**，不可當重複刪。判案場**不要逐格掃工地代號**（全工地版每個代號都掃得到，且會逾時），看檔名後綴或第一分頁 R2「專案名稱：」。
- 已同步：五支常用工具 `_跨月活件掃描` / `_狀態同步驗證` / `_請款進件查重` / `_批次歸檔` / `_案件執行回測`（共 10 處，實跑 PASS 79/0）。一次性 `_XXXX本輪回測.ps1` 依「只進不退」不改。

## 一、先 staging，不直接歸檔
- 順序固定：核對抓錯 → 在 `Downloads\` 開整理夾把該批檔集中 → 打計價本/修改單本 → **停下等使用者確認** → 他說「歸檔」才搬進工地。
- 🔴 原始檔若散在 Downloads 根目錄，**計價前第一步就先開夾搬檔**，不是打完才補。修改單同一套（來源檔＝差異表/預計追加表等），不只請款單。
- 放料進整理夾要**搬移不要複製**：Move-Item 直接搬，或 Copy 後立刻刪根目錄原檔。完成後驗「同一檔只在整理夾、根目錄 0 殘留」。
- 🔴 **同一批給的檔案用「時間窗」界定，不是用我覺得像不像**：先抓這次附檔的 `CreationTime` 時間窗，再對整個 Downloads 根目錄掃同窗，窗內全部視為同一批，一個都不准用「看起來不像」排除（曾漏收 24 份試驗報告＋承園合約.JPG）。
- 🔴 **使用者同一則訊息附的檔案＝他認定同屬一案**，即使內容看起來對不上（不同工地代碼/不同廠商/不同工項）也全部放進同一夾。真兜不起來就在夾內 txt 寫「這份跟案子對不上，出處XX，待確認」，不替他決定哪些不算數。
- 自檢固定加一項：對照時間窗清單逐一確認每個檔在整理夾、根目錄 0 殘留（不能只驗「我搬過的那幾個」）。
- 每夾都要有 `查核記錄.txt`。

## 二、命名與工地位置
- 請款夾名格式：`<工地代碼>、<項目>(<廠商>)#<期數>-請款單␣␣<計價月>`，例 `141E、主體結構混凝土材料(台灣水泥)#1-請款單  115.05`。
  ①開頭帶工地代碼＋全形頓號`、`，**不要**流水號 ②`#期數` 放在 `-請款單` **前面** ③「請款單」與計價月之間是**兩個半形空格** ④計價月用 `115.05` 點格式。
- 計價月＝**現在處理當月**，非單據工作月。期數從列控表項次或計價本該廠商上期+1 推，別留空。
- 工地根目錄 `Downloads\2.工地\`（🔴0812 起，見〇），案場：`1.141E 軟橋段案`／`2.141A 嘉科AP7P1-OFFICE`／`3.141C 台積電AP7P2-FAB結構`／`4.111D 台積電F18P7正式Office`／`5.121E 台積電F22P1-SUPPORT`／`6.131J`。
- 每案場 6 個類別子夾：`1.全程營運`／`2.請款單`／`3.入預算`／`4.修改單`／`5.施工週報`／`6.投入及計價`。
- 🔴 **請款單 → `案場\2.請款單\<月份>\`（有月份子夾，要放進去不是並排）；修改單 → `案場\4.修改單\`（無月份子夾）；施工週報 → `案場\5.施工週報\`（平的、檔名沿用原樣）。**
- 🔴 **`3.請款單 修改單` ＝「未完成/等待中」的暫存區**。使用者有時會把沒完成的誤歸進工地 → 我直接從工地拉回來就好，不用問。
- 待處理桶（🔴0812 起全在 `3.請款單 修改單\` 底下）：`0.5 採購議價`／`1.送出待簽核完成歸檔`／`2.退件`／`3.明天整理`／`4.核准`。問題夾可能在桶裡不在直屬層，找不到先 `-Recurse` 用單號定位，用磁碟物件的 `.FullName` 操作。

## 三、問題件
- 有問題/缺件 → 夾名加後綴 **`— 🔴問題標註`**，缺漏說明做成純文字檔放**夾內**（檔名同夾名），內容：狀態／🔴問題逐條附出處／還缺哪些附件／已完成什麼／核決層級／補齊後下一步。
- 問題件由**使用者自己退給工地**補件，夾就留在 Downloads 不動、計價本維持暫緩，等他補件回來或直接喊歸檔。
- 🔴 **動手前先查這案有沒有舊 🔴 夾**：`-Recurse` 搜 Downloads（含各桶）＋已歸檔的 `工地\<案場>\2.請款單\`，找同廠商+同期數+同項目。有就先讀舊夾的缺件說明.txt，把舊清單的必附件一併核對——不能只核這次新收到的檔夠不夠自洽。

## 四、進件查重（收到進件建好夾的當下就查，不是等歸檔）
- 退件幾乎必然會再進件，所以查重前移到**建夾當下**。
- 工具 `Downloads\agent\計價回測工具\_請款進件查重.ps1`：無參數＝掃 Downloads 根所有「…請款單 115.MM」夾；`-Key "項目(廠商)#期數"`＝單查。以**「項目(廠商)#期數」指紋**比對各桶＋`工地\*` 歸檔，列出同件舊夾。**只報告不自動刪。**
- 🔴 刪舊夾前三步：①核對新夾檔案已涵蓋舊夾 ②**舊夾的查核記錄.txt／缺件說明.txt／三聯單等佐證先搬進新夾保留**（那是前次審核心血）③`Remove-Item -LiteralPath` 刪舊夾。
- 刪的是「整理夾」不是工地原始來源檔的唯一副本；舊夾若有新夾沒有的原始檔，先搬過去再刪。
- 使用者自述「我也潔癖，希望我的下載乾淨」→ 看到 Downloads 亂可主動提整理，不用每次等他明講。

## 五、「歸檔」指令的語意
- 🔴 **使用者說「歸檔」＝這件他已確認沒問題**。收到指令直接做，不要再翻夾內的舊退件說明/查核記錄重新確認一次（他明講過「我叫你歸檔就歸檔 不要吵」）。
- 第一步看**計價本該分頁是否已填好金額+memo**——那是「已處理完」的第一手證據，計價本狀態本身就是最權威的。
- 只有在**計價本狀態與歸檔指令矛盾**時（分頁還空的、金額沒核過）才先回報，這時候回報是必要的不是囉唆。
- 對「— 🔴問題標註」夾收到歸檔指令：先**拿掉問題標註**（夾名後綴改回乾淨命名＋刪夾內問題 txt）→ 再照邏輯搬工地 → 同步把計價本呈現成 OK、清掉 M 欄暫緩/退件註記。缺件擋不擋是他裁量，他叫歸檔＝放行，**別再反問**。
- ⚠️ 唯一例外：打開夾發現的不是小缺件而是**硬矛盾**（標為請款單的檔內容根本是別的文件、金額三來源打架）→ 先講一句再搬，不把錯的內容歸進工地永久檔。
- 🔴 **模糊語句不算歸檔指令**：只有「歸檔」「搬進去」「可以搬了」這類明確動詞才算。反問句、「是不是」「怎麼」開頭的疑問句一律當成他在確認或糾正現狀，**不是授權**。看不準就照原句複述問清楚（曾把「你是不是少歸檔了」當默許，整組搬完又要復原）。
- 歸檔≠送呈：歸檔＝檔案歸位且結案，送呈動作仍留他按。

## 六、歸檔之後
- 🔴 歸檔＝結案＝OK → **該案的批次狀態記憶檔直接刪掉、MEMORY.md 索引行一起拿掉**，只留還沒歸檔的。收尾寫記憶前先問「這件歸檔了嗎」，歸檔了就不寫。
- 歸檔動作本身要包含「查重＋刪舊留新」，不是搬完那一夾就結束；驗證要驗到「這個案子在 Downloads 只剩一份」才算做完。
- 執行紀律：建夾/改名用 `-Path`＋`Test-Path` 驗證；搬檔 `Move-Item -LiteralPath` 不加 `-Force`，搬完驗夾內有／原位無；中文路徑一律 `-LiteralPath`。

## 回測測項
- T1 計價本打完前，原始檔是否已在整理夾、Downloads 根目錄 0 殘留？
- T2 有沒有用 CreationTime 時間窗全掃，而不是只收「看起來像的」？
- T3 收到「歸檔」時，有沒有去翻舊 txt 重新確認（不該做）／有沒有先看計價本狀態（該做）？
- T4 建夾當下有沒有跑 `_請款進件查重.ps1`？找到舊夾時，佐證檔有沒有先搬進新夾才刪？
- T5 歸檔完成後，該案的 `project_billing_*` 記憶檔與索引行有沒有一起清掉？
- T6 夾名是否符合 `代碼、項目(廠商)#期數-請款單␣␣115.MM`（兩個空格、點格式月份）？
- T7 🔴腳本/查核記錄/打法說明裡有沒有殘留舊路徑 `Downloads\工地`、`Downloads\公司`、`Downloads\2.退件` 等？活件判定有沒有改看 `3.請款單 修改單` 直屬而不是 Downloads 根目錄？
- T8 🔴同月同名不同後綴的成控月報，有沒有被誤當重複刪掉（應分開歸各自案場）？


## 🔴🔴🔴 工地母表版本管控：不准改名、不准跨訊息借版本、不准憑大小相近就判重複刪除（115.09.07）

**使用者原話**：「你看這兩個檔案 看起來外面名稱一樣 但裡面內容不一樣啊 你怎麼會分類亂分類呢」

**病灶三連**
1. **自行改名**：把 08:52 那則附的 `…-202609（用這份更新--用完要存擋關掉）(1).xlsx`
   （29,709,262 bytes／SHA 20DB63DA／61 分頁）改名成 `…-202609.xlsx` 複製進四個案夾，
   識別字被抹掉，與 08:49 那份 18.1MB 版同名不同內容（差 10MB、43 vs 61 分頁、前期累計 5,110,053 vs 10,839,453）。
2. **跨訊息借版本**：晨峰件他在 08:49 那則附 18.1MB 版，我卻放 08:52 那份；
   中誠件他那則**根本沒附母表**，我還自作主張塞一份進去。
3. **憑大小相近判重複而刪除**：正泰件他 08:51 附的 29,709,218 bytes／SHA 966F450D 那份，
   我判成「B類同一份檔的舊時點快照」刪掉，**無備份、無法還原**。

🔴🔴 **最嚴重的後果不是檔案亂，是查核結論被推翻**：用錯版本害我在晨峰件寫下
「工地端列控表 61 分頁全掃查無本案」並據以認定「無有效列控表」。換回他 08:49 那份後，
本案就在『雜項工程』r34（項次31｜115/7/29｜遮煙捲簾工程｜1式｜32,500｜稅1,625｜承辦林敬恩｜
SC012331-01｜備註「出工費及誤工費」），**三層算術全通**，memo 第4點檢附還漏了「列控表」。整條結論作廢重寫。

**三條鐵則**
- ① **檔名一字不改**：複製工地母表（或任何他給的檔）進案夾，一律保留原始全檔名，
  含「（用這份更新--用完要存擋關掉）」「(1)」這類看似冗贅的字串——那正是他分版本的唯一線索。
  與 [[feedback_no_abbreviating_user_given_names_0813]]「給的欄位值不准縮寫」同一精神，對**檔名**一樣適用。
- ② **用哪一版＝看他在哪一則訊息附了哪一份**：依「一次訊息附的多個檔案視為同一案邊界」，
  該則沒附母表就**不要自己塞**（[[feedback_only_do_whats_asked]]）；要引用別則的版本查證可以，
  但查核記錄要寫明出處是哪一則的哪個 SHA，**不可放進案夾冒充本件附件**。
- ③ **同月份母表不同版＝不同時點快照，不是重複檔**：刪除前一律先開檔比對
  【分頁數＋關鍵分頁的明細列＋說明區前期累計】三項，任一不同就是 C 類（不同輪次）要留，
  不可只看檔名／大小／月份就套三分法的 B 類直接刪。
  🔴 本次實證：18.1MB 版有 SC011505 玻璃工程(r33) 與 SC012331 遮煙捲簾(r34) 兩筆明細，
  28.3MB「用這份更新」版**這兩筆都被拿掉**、宗興零星合約區還改登 SC011515（單號差一碼）——
  這種差異**用大小根本看不出來**。

**查無結論的舉證責任**：要寫「列控表查無本案」之前，必須把該件**所有可得版本都掃過**，
並在查核記錄逐一列出「檔名＋bytes＋SHA 前12碼＋分頁數＋掃描關鍵字＋結果」。只掃一版就下「查無」＝FAIL。

**落地**：兩本『請款單打法說明』已同步新增 **■四十二**（141A r145／141E r154，_規則同步回測 PASS 188/FAIL 0）。
**回測測項**：T1 案夾內母表檔名與原始給檔逐字比對(-ceq)；T2 案夾內母表 SHA＝該則訊息所附那份
（原檔不可得則查核記錄須有揭露段）；T3 出現「列控表查無」字樣者同段必須有≥1組檔名+bytes+SHA+分頁數佐證；
T4 案夾內不得有該則訊息未附、且查核記錄未註明來源的母表檔。

相關：[[reference_billing_book_format_rules]]、[[feedback_only_do_whats_asked]]、[[feedback_late_billing_alert]]、[[reference_excel_ps_traps_0806]]、[[feedback_memory_manual_format_0805]]

---

## 🔴🔴🔴 115.08.27 教訓：「刪舊留新」不能只看檔案時間，要先驗新版有沒有涵蓋舊版

**病灶：** 同一天工地給了兩次 `嘉科AP7P1-OFFICE結構工程零星列控表(工地端)-202608...xlsx`，08:42 一份、08:54 一份，檔名完全一樣、SHA256 不同。我照 ■二十八「B類＝同一份檔案的舊時點快照 → 新版涵蓋舊版 → 直接刪」把 08:42 版刪掉。**結果兩版是分岔的工作版，不是新舊關係**：
- 「材料試驗」頁：08:42 版**有**竣葦#10（33,000）、預算總計 2,345,632；08:54 版**沒有**、預算退回 1,345,632
- 「雜項工程」頁：08:42 版**沒有**訊達（796,480）、預算總計 14,717,937；08:54 版**有**、預算 11,817,937

刪掉後才發現 08:54 版反而少了竣葦那件的唯一登錄依據。內容我當時已 dump 成 txt 才保住（存進案夾當存證），但原始 xlsx 只能請工地重給。

**🔴 鐵則（補進 ■二十八 三分法之前）：把檔案歸進「B類舊快照」之前，一定先做**涵蓋性驗證**——打開新版，確認**本案本期那一列/那一頁的資料在新版裡真的存在**。驗不過就不是 B 類，兩版都要留，並改名標時間（如 `...-202608(0842版).xlsx`／`(0854版)`）＋在查核記錄寫明兩版差異與採用哪版。

**🔴 連帶新現象：** 工地端零星列控表會同日出現多個分岔工作版（多人各自編輯同一份母表再各自下載），**不同分頁各自缺對方的資料、預算數互相打架**。拿到工地端母表的第一動改成：先跳到本案本期那一頁，確認本期有沒有登錄、預算三數（總計/前期累計/餘額）自不自洽，再決定用哪版。

**回測測項：** T9 刪除任何「同名不同 SHA256」的檔案前，記錄裡有沒有一段「涵蓋性驗證」（列出新版中本案本期那一列的實際內容）？沒有＝FAIL。

---

## 🔴🔴🔴 115.09.08 收緊 ■四十二 T4：「有註明來源」不是把別案檔案放進案夾的通行證

**使用者原話：** 「你個是不是放錯資料夾了啊」「為啥會放錯資料夾啊」

**病灶：** 115.09.08 那一批七件裡，`03-嘉科AP7P1-OFFICE結構工程零星列控表(工地端)-202609.xlsx`
是 141A-SC012347（常証）那批的第 03 號檔（01-說明／02-報價簽單／03-列控表／04-施工照片 一整組），
10:25:57 落在 Downloads 根目錄時使用者還沒指名歸屬。我用它查證 SC012290（鑫鎂）與 SC012119（高信）
有沒有登進工地端母表——**查證本身是對的**——但用完後**自己決定 Copy 一份進那兩個案夾「留查證痕跡」**。

**🔴 最糟的不是不知道，是知道了還繞過：** 我當時在兩件的 M 欄與查核記錄都寫了
「※ 此母表非你這兩則訊息直接附的檔……為留下查證痕跡而複製進夾」，
等於**拿既有 T4 的「查核記錄有註明來源」當成放行條件**，替自己的自作主張背書。
這與 0907 晨峰案「該用哪一版＝看他在哪一則訊息附了哪一份，不可拿別則的版本代替」是同型錯的變體：
那次是拿錯版本，這次是把**還沒指名歸屬**的別案檔案塞進兩個不相干的案夾。

**🔴 收緊後的鐵則（取代舊 T4 的寬鬆解讀）：**
1. **案夾內只放使用者那則訊息附給該件的檔案**（＋我為該件從桌面複製的行通表母檔，那是明文規定要入夾的）。
   工地端零星列控表這類共用母表，**只有在它是該則訊息的附檔時才進夾**。
2. **查證用得到 ≠ 檔案要進夾。** 要留痕跡就在查核記錄的📎查證明細寫
   「檔名＋bytes＋SHA 前12碼＋分頁數＋掃描關鍵字＋結果＋原檔現在放哪個案夾」，
   **檔案本身不複製**。查證結論的效力不因檔案不在夾內而減損。
3. **「我有註明來源」不構成放行理由。** 註明是揭露義務，不是許可。
   寫得出「這不是他給本件的檔」就代表我已經知道不該放——知道還放，比不知道更糟。
4. **實質檢驗：** 放進去有沒有用？本案兩件 memo 第4點檢附都沒列列控表（查無本案），
   夾內留 19MB 的別案母表只會害下一個接手的人誤判本件有列控表。**沒用還會誤導＝不准放。**

**How to apply:** 歸檔前最後一動，逐夾點名：這個檔是他哪一則訊息附的？答不出來就拿掉。
自檢時間點＝每件建完夾、寫完查核記錄要收尾之前，不要等被問。

**回測測項（取代舊 T4）：**
- T4a 案夾內每一個檔案，都能對應到使用者某一則訊息的附檔（或是規定要入夾的行通表母檔）？
  對應不上＝FAIL，**查核記錄有沒有註明來源都一樣 FAIL**。
- T4b 查核記錄裡出現「為留查證痕跡而複製」「非你這則訊息直接附的檔」這類自我開脫語句＝FAIL。
- T4c 用到但不入夾的共用母表，📎查證明細段有沒有寫出「原檔現在在哪個案夾」？

**落地：** 兩本『請款單打法說明』■四十二 待同步（115.09.08 計價本被使用者開著，等關檔後補）。

### 🔴🔴🔴 115.09.08 使用者定版（逐字）：「我給你資料多少 就多少放進資料夾」
案夾內容＝**他那則訊息給的檔案，一份不多、一份不少**。
- 不多：任何他沒給的檔案都不進夾——別案的、根目錄撿的、我為了查證去複製的，一律不放。
- 不少：他給的每一份都要進夾，不可因為「看起來對不上本案」就排除（■十七既有規則）。
- 我方產出的例外只有兩個，且都是明文規定：①每夾必附的 `查核記錄.txt`
  ②同件「刪舊留新」時從舊夾搬進來的該件歷史（舊查核記錄／作廢版佐證，須改名標作廢）。
- 🟠 行通表母檔入夾（[[reference_attachment_checklist_0806]]）與本條有交界：
  memo 第4點檢附有列「行通表」時夾內才需要有，否則不放。此點 115.09.08 已主動向使用者揭露待確認。
