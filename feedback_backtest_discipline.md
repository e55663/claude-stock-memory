---
name: feedback_backtest_discipline
description: 🔴🔴使用者要求建立回測且「沒做好就持續做到好」;桌面兩支回測(_回測.ps1行通表52項/_追蹤報表回測.ps1);回測要能抓出自己的假PASS;PowerShell變數不分大小寫踩雷
metadata: 
  node_type: memory
  type: feedback
  originSessionId: d01b7fb3-8186-4c01-978a-f186d615da81
  modified: 2026-08-05T08:33:00.473Z
---

🔴🔴 **(115.07.21 使用者要求)** 「我要建立回測哦，如果沒做好就要持續做到好的那種，直到做好。」

## 現有兩支回測（做完任何相關作業就要跑）

| 腳本 | 位置 | 內容 |
|---|---|---|
| `_回測.ps1` | 桌面`行通表\` | 52項：16本母檔逐本項數比對／欄位完整性／季別類別涵蓋／**11個已知案例金標準**（長安點工應查無、曹新泰散裝應查無、台泥/祥欣/國廣應查無…）／索引新鮮度 |
| `_追蹤報表回測.ps1` | `Downloads\agent\計價回測工具\`（115.08.05從桌面搬入，見下方遷移記錄） | **78項(115.07.21傍晚從43項擴充)**：分頁齊全／10欄順序名稱一致／請款項目無空白／**無非文字或過短異常值**／日期格式115.MM.DD／輸出「已查證 vs 待補」進度／🆕T5備註只四種或空白／T6合計列存在+件數+金額+四狀態口徑／T7金額欄不可存成文字／T8框線抽驗／T9打法說明分頁要寫到新規則。新規則**只驗當月那本**，舊月份自動略過T5~T9 |

🔴🔴**(115.08.05使用者要求)『桌面\新增資料夾』整個搬進『Downloads\agent\計價回測工具』**（agent＝Claude Code這個session的工作目錄；**資料夾實際名稱是「計價回測工具」不是「新增資料夾」**），桌面不再放。
🔴 **同日連帶清乾淨的三處（搬完才算完成）**：①兩本『請款單打法說明』所有寫「桌面『新增資料夾』」的條文已一次改成新路徑（141A:A103/A121/A130/A181/A189/A193/A203/A207；141E:A72/A128/A137/A188/A196/A200/A210/A214），並各新增一條遷移規則（141A A217／141E A224）。②`_規則同步回測.ps1` 的 T4「回測腳本齊全」原本只列 3 支，搬家不會被抓到 → 已補成 11 項（含 `_計價本回測.ps1`／`_計價本回測_基準.txt`／`_0804本輪回測.ps1`／`_清Excel殘留.ps1`），並新增 T5 驗「兩本計價本與月報表仍在桌面根目錄」。③桌面同時新增了『計價本』夾（101I/111D/121E/131J/131L/141C 等**其他工地**的計價本）與『行通表』夾；🔴**我在跑的 01.計價-141A／01.計價-141E 與月追蹤報表仍在桌面根目錄，沒被搬進『計價本』夾**，腳本路徑不用改。內含：`_0804本輪回測.ps1`／`_清Excel殘留.ps1`／`_規則同步回測.ps1`／`_計價本回測.ps1`／`_計價本回測_基準.txt`／`_請款進件查重.ps1`／`_追蹤報表回測.ps1`，以及**`選股說明.txt`、`選股對帳紀錄.txt`這兩份選股流程核心檔也在裡面一起搬走了**。
✅ **115.08.05 已改**：`.claude\hooks\stock-gate.ps1` 原本寫死「讀桌面『選股說明.txt』／『選股對帳紀錄.txt』」，已改成 `Downloads\agent\計價回測工具\`（原檔有 BOM，寫回時保留 BOM）。同時掃過全部記憶檔，17 個檔裡的舊桌面路徑一次改齊；`co-rule-gate.ps1` 的兩處桌面引用指的是 02.修改審查說明與月追蹤報表，那兩者**仍在桌面**，不用改。

🔴 **規則一改，回測要當場跟著加測項**：今天改完「備註四種/合計列/框線/打法說明同步」後，舊的43項全PASS——但那是因為新規則一項都沒被驗到。**沒涵蓋新規則的全PASS＝假PASS**。
🔴 **加完測項要拿反例證明它會FAIL**：本次用六月那本套T5，抓到38列違規＝測項有效；但T8框線我沒找到反例(六月本來就有框線)，**這種只能算狀態宣告，要老實講沒驗到會不會FAIL**。

## 🔴 回測必須能抓出「假PASS」

第一版 `_追蹤報表回測.ps1` 報「待補0、已查證100」是**假的**——因為它比對的待補字串跟實際寫入的不一致，把待補列誤判成已完成。**回測寫得太寬鬆＝比沒有回測更糟**（會讓我誤以為做完了）。修正後加了 T3b：檢查值的型別與長度，才抓出真相。

**教訓**：回測的判定條件要能對抗「我自己寫壞的那種錯」，不能只驗「有沒有值」。

## 🔴🔴 PowerShell 踩雷：變數名不分大小寫

我定義了 `$TODO = "待補:需調閱…"` 當待補文字，同一個迴圈裡又用 `$todo = 0` 當計數器並 `$todo++`——**PowerShell 的 `$TODO` 和 `$todo` 是同一個變數**，所以待補文字被覆蓋成數字，寫進 Excel 的變成 0/1/2/3/4…，兩份報表共 57 列被寫錯值。

**How to apply**：
1. 腳本裡**常數與計數器絕不可只差大小寫**，常數用 `$標準待補`、計數器用 `$cntTodo` 這種明確區隔的名字
2. 批次寫完 Excel 後，**一定要抽查實際儲存格的 Value2 型別**（不是只看有沒有值），數字型別出現在文字欄＝被覆蓋了
3. 呼應 [[feedback_verify_after_batch_ops]]：指令沒報錯 ≠ 寫對了

## 🔴🔴(115.08.05建置) 新增兩支「操作型」腳本，不是回測，是防手滑的執行工具

使用者原話：「我當然希望我每天花的時間是有回報的，然後可以越跑越順」——今天翔博案總表插錯欄位(手算colBase打成1，應該是9)就是「每次手刻COM腳本」的代價，同一批東西每次都重新手寫，越複雜越容易錯。

- `Insert-SortedRow`／`Remove-TotalRow`／`Move-PendingToApproved`／`Test-SortIntegrity`（`Downloads\agent\計價回測工具\_總表操作函式庫.ps1`）：總表待核↔核定搬移的欄位改成查表(`$Global:TotalSheetColMap`)，不再手算數字；`Move-PendingToApproved`一次做完「移除+插入」；`Test-SortIntegrity`驗四區塊排序無重複，這就是今天實際抓出翔博卡在錯欄位的方法。
- `_批次歸檔.ps1`（同資料夾）：填一份`$cases`清單，一次做完「搬資料夾→總表搬區→安全存檔→跑既有三支回測」，整批只開一次Excel實例(減少今天多次撞到的RPC斷線)，帶`-DryRun`可以先預演不動真檔案。
- 🔴🔴這兩支腳本第一版把函式/變數名取成中文，存檔沒BOM，PowerShell 5.1當ANSI讀導致整支語法解析失敗(30+條錯誤)——跟[[feedback_chinese_string_powershell_traps_0804]]同一個雷又踩一次。已改用純英文命名+顯式補BOM+`Parser::ParseFile`靜態檢查後修好，往後任何新.ps1函式/變數名一律純英文，這條要記牢不是記BOM本身。
- ⚠️尚未在真實批次跑過(只做過語法檢查+函式載入測試，沒有對真實Excel執行過完整流程)，下次真的有案子要歸檔時是第一次實戰考驗，跑完要回報有沒有問題再繼續信任這支腳本。

相關：[[feedback_hangtong_existence_gate]]、[[reference_monthly_tracking_report]]、[[feedback_no_perfunctory_work]]
