---
name: feedback_backtest_discipline
description: 回測制度手冊(整合3檔):A層驗規則有沒有寫/B層驗有沒有真的執行;每批另寫本輪專用回測;假PASS與假FAIL一樣糟;七支腳本清單與位置
metadata: 
  node_type: memory
  type: feedback
  originSessionId: d01b7fb3-8186-4c01-978a-f186d615da81
  modified: 2026-08-06T03:01:37.091Z
---

# 回測制度

使用者原話：「我要建立回測哦，如果沒做好就要持續做到好的那種，直到做好。」「我當然希望我每天花的時間是有回報的，然後可以越跑越順。」

## 🔴 回測分兩層，A→B 是必要順序不是二選一
- **A 層＝打法說明文件裡有沒有寫這條規則**（`_規則同步回測.ps1`）。這是前提，不是終點。
- **B 層＝有沒有真的照規則執行**（`_計價本回測.ps1`／`_追蹤報表回測.ps1`／`_案件執行回測.ps1`）。
- 🔴 **「已經寫進打法說明」不代表「已經在驗有沒有執行」**。改完/加完規則先確認 A，再想這條規則的執行結果能不能用 B 層驗到；驗不到的就是覆蓋缺口，要主動補。
- 實例：打法說明第五條(開整理夾)、第十五條(每案附查核記錄.txt) A 都有寫，但沒有任何回測驗它們有沒有執行 → 0806 銓億#1 做完全部查核卻忘了開夾寫記錄，就是踩到這個缺口。補上 `_案件執行回測.ps1` 首跑就抓到 **7 個現存案夾真的缺查核記錄.txt**。

## 腳本清單（`Downloads\agent\計價回測工具\`，115.08.05 從桌面搬入）
| 腳本 | 層 | 內容 |
|---|---|---|
| `_規則同步回測.ps1` | A | 驗兩本打法說明有沒有寫到規則、關鍵字涵蓋、回測腳本齊全(11項)、兩本計價本與月報表仍在桌面根目錄 |
| `_計價本回測.ps1` | B | 讀計價本實際內容比對規則（T9 驗「J欄有金額就必須有扣款行」、T12 驗「分頁 tab 不得有顏色」） |
| `_追蹤報表回測.ps1` | B | 78 項：分頁齊全／10欄順序／請款項目無空白／日期格式／T5備註只四種／T6合計列＋件數＋金額／T7金額不可存成文字／T8框線／T9打法說明有寫新規則。新規則**只驗當月那本** |
| `_案件執行回測.ps1` | B | 掃 Downloads 根＋四個母夾的案夾，驗①查核記錄.txt 在不在②夾內非空③記錄時間早於原始檔(抓時序異常) |
| `_回測.ps1` | — | 行通表索引 52 項（在 `桌面\行通表\`），含 11 個已知案例金標準 |
| `_精簡版覆蓋率回測.ps1` | A | 驗打法說明壓縮後沒漏規則概念（141A+141E 共 136 項） |
| `_記憶壓縮回測.ps1` | — | 驗記憶檔合併沒掉硬 token（見 [[feedback_memory_manual_format_0805]]） |
| `_總表操作函式庫.ps1`／`_批次歸檔.ps1`／`_請款進件查重.ps1`／`_清Excel殘留.ps1` | 工具 | 非回測，是防手滑的執行工具 |

⚠️ `_批次歸檔.ps1` 尚未在真實批次跑過（只做過語法檢查＋函式載入測試），下次真要批次歸檔是第一次實戰，跑完要回報再繼續信任。

## 🔴 每批作業另寫「本輪專用回測」
- 既有腳本只驗**通則**，不會驗我這一輪講過的具體數字與落地動作。**全綠不等於這批做對了。**
- 每批收尾另寫一支，把自己講過的每個數字、每個落地動作，**回到來源檔重新驗**（數字一律重算，不採信我自己寫進計價本的值）。
- 0804 本輪回測第一次跑就抓到我三個錯：①行通表門檻沒過卻在檢附欄列「行通表」（核決者會誤以為門檻關過了）②檢附列了行通表但案夾內沒放該檔 ③測項寫太鬆指不到位。
- 定案：門檻**過** → 行通表母檔複製進案夾＋檢附列「行通表」（兩件事同時做）；門檻**沒過** → 檢附一律不列。

## 🔴 假 PASS 與假 FAIL 一樣糟
- **假 PASS**：規則改了但沒加測項 → 舊測項全 PASS 是因為新規則一項都沒被驗到。**沒涵蓋新規則的全 PASS ＝假 PASS**。第一版 `_追蹤報表回測` 報「待補 0／已查證 100」也是假的（比對字串跟實際寫入不一致）。
- **加完測項要拿反例證明它會 FAIL**：用六月那本套 T5 抓到 38 列違規＝測項有效；T8 框線找不到反例＝只能算狀態宣告，**要老實講沒驗到會不會 FAIL**。
- **假 FAIL**：逐格 `.Cells.Item($r,$c).Text` 讀大檔，COM 會讀到一半半死並**靜默回空字串**（不報錯），後段全部誤判成「找不到」。徵兆＝「141A 有 0 條／141E 有 0 條」＋結尾 RPC 失敗。
  修法：整欄一次抓 `$s.Range("A1:A$lastRow").Value2` 陣列不逐格；加健全性自檢（讀到字數低於門檻就中止並印「COM 讀取失敗，非內容缺漏，請關掉 Excel 後重跑」）。⚠️ 門檻要跟著內容量調整——打法說明壓縮後正常只有 8~9 千字，原本寫死 10,000 會誤判成讀取失敗，已調降 3,000。
- 🔴 全 FAIL 或整批 0 筆**先懷疑讀取失敗**，不要直接認定內容缺漏。COM 掛掉徵兆：`0x800706BE`／`0x800AC472`／`0x800A03EC`／「有 0 條」→ 殺掉無視窗 Excel、等 6~10 秒、重試。
- 🔴 **回測報 FAIL 時，先懷疑是不是檢查腳本自己寫錯**，別急著去改本來正確的資料。
- 測項要能指出**該改哪一格**；只說「有問題」不算合格（D2 原寫「memo 不得出現『行通表,符合』」會同時命中單價行與檢附行，拆成 D2a/D2b 才指得到位）。

## 🔴 腳本自己的雷
- 常數與計數器**絕不可只差大小寫**（`$TODO` 與 `$todo` 是同一個，曾把待補文字覆蓋成數字，兩份報表 57 列寫錯值）。常數用 `$標準待補`、計數器用 `$cntTodo`。
- 批次寫完 Excel 後**抽查實際儲存格的 Value2 型別**（不是只看有沒有值）——數字型別出現在文字欄＝被覆蓋了。
- 新 .ps1 的函式名/變數名一律**純 ASCII**＋補 BOM＋`Parser::ParseFile` 靜態檢查（中文命名＋無 BOM 曾讓兩支新腳本 30+ 條解析錯誤）。
- 詳見 [[reference_excel_ps_traps_0806]]。

## 回測測項
- T1 這輪改了規則，有沒有當場加對應測項？加完有沒有拿反例證明它會 FAIL？
- T2 全 PASS 時，有沒有確認新規則真的被某個測項涵蓋到？
- T3 出現整批 0 筆/全 FAIL 時，有沒有先驗 COM 讀取健全性再下結論？
- T4 這批作業有沒有另寫本輪專用回測，數字有沒有回來源檔重算？
- T5 A 層寫進打法說明的新規則，B 層驗得到嗎？驗不到有沒有主動補？

相關：[[feedback_hangtong_existence_gate]]、[[reference_monthly_tracking_report]]、[[feedback_no_perfunctory_work]]、[[feedback_gate_applies_to_my_own_output]]、[[feedback_status_sync_and_totals_0806]]
