---
name: reference_model_cost_and_dispatch_0806
description: 模型選擇與額度手冊(整合4檔):切模型四層權限/額度三筆成本(重複注入最貴)/子代理派工三條件與禁寫清單/gate-dispatcher設計/汰舊換新原則
metadata: 
  node_type: memory
  type: reference
  originSessionId: 365bbf1a-11d3-4cf1-8927-79cb45fcc87f
  modified: 2026-08-06T03:13:19.281Z
---

# 模型選擇、額度與派工

## 一、模型選擇
- **工作類（請款核對／計價／修改單／審核抓錯）→ Opus**；雜事（搬檔／改字／跑固定腳本／撈資料）→ Sonnet 夠。
- 🔴 病根不是單價：Sonnet 在抓細節任務出錯多，「使用者抓到→重做→回測」每次都是額外好幾輪，**token 被乘上去**。Opus 一次做對機率高，對高風險核對總額度反而更省。
- **切模型四層權限**：①執行中視窗的模型＝**只有使用者能換**（`/model`），我無法自己切 ②預設模型＝我能改（`settings.json` 的 `"model"`），🔴不得未經同意就改 ③**子代理模型＝我能指定**（Agent 工具 `model` 參數）——這是最有用的一層 ④Codex 模型＝我能改（`~\.codex\config.toml`）。
- 🔴 **切模型的隱藏成本**：prompt cache 各模型獨立，同一視窗中途 `/model` 切換，前面的 context 要在新模型重跑一遍**反而更花額度**。規則＝**開新視窗時就決定，不要做到一半切**。
- 🔴 **7/20 的 Sonnet 結論不可套到 Sonnet 5**（當時測的是舊版）。要換之前先跑對照組：拿一件已審完、答案確認過的案子從頭跑，用現成測項比對漏哪幾項。
- 🔴 **對照試已中止、至今沒有任何有效分數**（0805 使用者決定不重測，測試場已刪、評分表全空）。現有證據撐不住「Opus 一定比較穩」——0803 閘門設太鬆那次按預設推算很可能就是 Opus 跑的；唯一一次從零跑完整案且全中的是 **Codex**。分層結論仍成立，但那是**罰則不對稱的推論，不是實測排名**。
- 🔴 **安達#1 與平安#15 已不能再當測試場**：答案寫在 MEMORY.md 索引行，而索引每個 session 開場強制載入，新視窗一開就被污染。要重測必須換全新案子（答案卡完整但索引行不寫細節），三邊時間錯開，每邊跑完先存 `報告-<模型名>.txt`。
- 🔴 **回測補不了的那塊仍歸 Opus**：memo 定版、規則沉澱、新測項設計、行通表門檻與核決層級判定、掃描件手寫判讀、選股判盤——既有回測驗不到這些。
- Opus 5 已於 2026/07/23~24 發布（此事在知識截止後，新 session 會不知道要主動查證）：1M 脈絡＋xhigh 推理＋每回合切 low/med/high 控成本，價格與 4.8 同。若 `/model` 看不到＝視窗在更新前開的，關掉重開或手動打 `claude-opus-5`。

## 二、額度真正花在哪（三筆，不是兩筆）
1. **開場稅**（一次）：CLAUDE.md ＋ MEMORY.md 索引＋骨架。`/clear` 不退額度，只清 context；每次 clear ＝重付一次。
2. **讀檔**（一次）。
3. 🔴🔴 **每輪固定注入 × 輪數**（最容易被忽略，會隨對話長度線性成長）：三支 hook 曾每輪無條件噴 2,271 字，60 輪 session ＝約 10 萬 token，內容還跟當天在做的事無關。隱形代價是**把 context 撐爆→提早壓縮→被摘掉的正好是前面幾案的查核細節**。花錢買沒用的字，然後那些字把有用的擠掉。
- 🔴 被問「怎麼省額度」時的順序：**先量每輪固定注入量**（跑 hook 看輸出字數）→ 掃描件頁數 → 段落式 /clear → 雜事切 Sonnet。
- 🔴 工作節奏：**處理一批案件→告一段落才收尾＋clear**，不要每單檔清。獨立案之間仍要清（避免數字交叉污染），但抓在案群處理完或對話變慢時。
- 一個舊案檔只要①不在索引②沒被讀到 → 對額度完全隱形。**封存 vs 刪，額度差 ≈ 0**（差別只在硬碟）。

## 三、規則層 vs 案件層（使用者最在意：只要規則記好）
- **規則層**（reference_／feedback_）＝新規矩、長官要求改的 → 進索引、記紮實、**覆蓋舊的不堆疊**。數量會收斂。**這是資產，唯一要顧好的。**
- **案件層**（project_*）＝某案某期的流水數字 → 不佔索引；他的工地請款是「丟檔→照規則做→歸檔→不回頭」的一次性場景，案件快照幾乎無未來 recall 價值。
- 目標：讓開場成本只跟「規則數」長（會停），不跟「案件數」長（無限）。
- 🔴 **汰舊換新**（0805 使用者原話：「記憶的部分要汰舊換新，只要新的是對的，這樣就對了」）：規則更新且驗證正確就**直接改寫或刪掉舊的那段**，不要寫成「原本是 A，後來改成 B」的考古層。判準＝**這段舊內容還會影響未來判斷嗎？** 會→留；只是紀錄「曾經如此」→刪。
  **例外**：教訓型記憶要保留「當初怎麼錯的」——**錯誤過程有預測力，過時的狀態沒有**。

## 四、子代理派工
成本結構是三段：①我寫任務簡報（Opus 價，背景要重講）②子代理全程跑（Sonnet 價，但它要自己重讀 CLAUDE.md/MEMORY.md/案子檔＝重複付費）③我讀回報＋驗收（Opus 價）。**省不省取決於第 2 段佔多少，不是派出去就一定省。**
- 划算的形狀＝**讀很多、回報很少**（掃全部案夾列缺件、跑回測回報 FAIL 清單、盤點、查索引 CSV）。
- 不划算＝小事（三個工具呼叫就完）、需要來回對話、需要我既有 context 才判斷得了的（context 重建＝同一份資料付兩次）。
- 🔴 **派工三條件（同時成立才丟）**：①有明確成功判準（回測過不過／檔案在不在／數字對不對）②成果能快速驗收不用重讀原始資料 ③做錯代價低（可回復、不會送出去）。**任一不成立＝我自己做。**
- 🔴 **不准用「先丟 Sonnet 試，不行再 Opus 接手」**——兩邊的錢都付、多一輪來回、中途產出可能要清掉重來。**派錯的成本是雙倍。**
- 🔴 **依使用者規則，沒他開口不主動叫子代理。**
- 🔴🔴 **子代理禁止寫入清單**（權限是 bypassPermissions 會繼承，寫得動檔案；0731 已發生過重建檔案蓋掉使用者改好版本的事故）：
  - **一律不准動**：計價本 `桌面\01.計價-*.xlsx`、修改單本 `桌面\02.修改審查說明-*.xlsx`、任何交付檔
  - 只做：讀取盤點，以及可回復的搬檔
  - 要寫進那三種檔案的，一律 Opus 親自來
- 可丟 Sonnet：歸檔搬檔／批次改名／清重複夾／跑既有回測回報 FAIL／掃夾盤點缺件／查行通表索引／把**已定案**的數字抄進 Excel。
- 必須 Opus 自己做：memo 定版、請款項目欄寫法、行通表門檻判定、核決層級、扣款拆自辦代辦、掃描件手寫判讀與簽名有無、規則沉澱寫兩本打法說明、設計新測項、選股判盤。

## 五、gate-dispatcher（每輪注入的省法）
- `\.claude\hooks\gate-dispatcher.ps1` 取代三支無條件噴的 hook（`session-time.ps1` 不動）。它**不含任何閘門文字**，只做判斷後呼叫原本那幾支，閘門內容零風險。
- 四層設計：①偵測（掃這輪 prompt 比對關鍵字組）②**黏著**（命中一次寫進 `hooks\state\<session_id>.txt`，整場維持開啟，之後打「好」也不會關）③手動開關壓過自動（`股票模式`／`工作模式`／`閘門全開`／`關股票`／`關工作`）④**fail-open**（解析失敗或例外一律全噴＝退回改版前行為，絕不會比原本少）。
- 刻意避開的誤觸：股票關鍵字**不放單一「股」字**（「銘亮股份有限公司」會誤觸）、**不放「台積電」**（計價本檔名就叫 `01.計價-141A 台積電AP7P1-Office.xlsx`）。
- 0806 新增第三組：**tier2 第二層索引提醒**（投資/環境系統/個人生活關鍵字 → 提醒讀 `INDEX_ALL.md`）。
- 回測：`hooks\_dispatcher-backtest.ps1` 25 項 25 PASS（該場實測**省 62.5%**）；`計價回測工具\_tier2閘門回測.ps1` 7 項。**規則一改測項要跟著加**，否則假 PASS。
- `hooks\state\dispatch.log` 每輪記一行判斷結果（只寫檔不輸出，token 成本 0），判錯了回頭查這裡。
- 🔴 **未解**：`co-rule-gate.ps1` 第 2 行仍寫「新增項目議價 20~100萬/100~300萬＝執行副總」，與 [[reference_approval_authority_table]]（工地流程沒有執行副總這關）衝突＝每輪餵一條已被推翻的規則。已回報，使用者尚未裁示，**我沒動它**。

相關：[[feedback_work_habits_and_output_0806]]、[[feedback_memory_manual_format_0805]]、[[feedback_blindtest_system_reminder_contamination_0804]]、[[feedback_claude_codex_shared_workflow]]、[[feedback_never_overwrite_user_edited_file]]、[[reference_scanned_audit_cost_and_toolchain]]
