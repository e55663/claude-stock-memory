---
name: reference_billing_book_format_rules
description: 計價本格式鐵則(一行式):memo逐字/整列@文字格式/B4A4H4公式/標楷體/期數舊在上/新分頁放範本(6)旁/建頁當下登總表;含回測測項
metadata: 
  node_type: memory
  type: reference
  originSessionId: 8a007757-43c3-4bd8-8838-2d576d43a25a
  modified: 2026-08-05T08:59:27.650Z
---

# 計價本格式鐵則

## 現行規則（動計價本前只讀這層）
- 使用者給的 memo 逐字一模一樣進 Excel，不改寫、不重排、不精簡。
- B4(呈核memo)、A4(夾名)、H4(含稅) 一律公式，絕不打死純文字。
- B4 公式動態欄連回格子：單號`&C4`、項目`&D4`、廠商`&E4`、期數`&F4`、金額`&G4`、檢附`&I4`、扣款`&J4`；換行用 `&CHAR(10)&`。**寫死＝側欄被污染時 memo 照樣正常、錯誤隱形**。
- H4 公式：`=ROUND(SUBSTITUTE(G4,",","")*1.05,0)`；不認稅/含稅項用 `=SUBSTITUTE(G4,",","")*1`。
- 整列資料欄一律文字格式 `@`：C單號/D項目/E廠商/F期數/G金額/I檢附/J扣款/K日期；總表資料欄同樣 @。只有 A/B/H 是公式不動。
- G4、J4 是**文字含逗號**（`147,000` 非數字），扣款有小數保留兩位不四捨五入。
- COM 設 @ 的順序：先 `NumberFormat='@'` 再把 `.Value2` 寫回 `.Text`（先格式後寫值才會真的變文字），`HasFormula` 的格跳過。
- 全書字體「標楷體」，只改 `Font.Name` 不改 Size；🔴加超連結後 Excel 會套回 Microsoft JhengHei UI，加完務必改回標楷體。新增分頁/總表列/堆新期後都要設。
- 期數排列：最上排 row4＝最舊，越往下越新；新期往下新增，不覆蓋舊期。
- 分頁 tab 順序：新分頁**插在範本(6) 旁邊＝最前面**，越右邊越舊。
- 建任何新分頁＝當下就在總表補一列：PO走左區(A-D)、SC走中區(F-I)，填單號+項目+廠商，**項目欄做超連結指到 `真實分頁名!A1`**，字體框線比照既有列。判重唯一鍵＝單號。
- 期數認**廠商請款單自己寫的「第X期」**，不是排到第幾列；下筆前先 dump 該分頁 F 欄確認連續無跳號無重號。
- 下筆前用 F 欄定位「F＝這期期數」那列才更新；確認是全新期數才往最下新增；🔴絕不在沒確認 F 的情況下改某列 I/J/K。
- 請款單寫 #X 但分頁只到 #(X−2)、中間真缺一期 → 停下來問；既有列金額/標號錯位是另一條線，不拿來擋本期。
- 既有分頁加新期：先 `Range('B4:N4').Copy()` → `Range('B5:N5').PasteSpecial(-4104)` 複製整列格式再改 F/G/K/B；⚠️上一列 I 是空的複製下來也空，memo 吃 `&I&` 會變空要補回。
- memo 第1點固定：`1. 本期(114/11/01~11/30)…計價金額…元(未稅)`，區間從列控表施作日期 min~max 抓，不掰。
- memo 第3點標籤寫「**本期項目:**」，不寫「本期施作項目」。
- 署名固定 `職 羅慶人 敬呈 115.MM.DD`（日期＝問當天）；只有追溯不到的舊期補登可空。
- 扣款行預設 `5.本期代扣款{含稅金額}元(含稅)，廠商已簽認。`；J欄填含稅值。付料類內扣/代辦才用未稅＋未簽認。
- **沒有扣款(J4空/0) → 整條扣款行省略，後面「呈核」編號往前遞補**（無扣款版＝5行：計價金額/相符/本期項目/檢附/呈核）。
- 點租工/機具/吊車才寫「單價與X季行通表，符合」；**工程項目(鋼筋成型加工/鋼網牆/高壓水泥磚)不寫單價行**，單價對的是合約報價單。
- 檢附照實際給的檔列（給合約就寫合約，如 `列控表,合約,請款單,磅料單`）。
- 問題/退件件**保留分頁不刪**（刪了改回還要重建＝白燒額度）；memo 寫問題原因不寫「符合」，金額留廠商請款值。⚠️tab 標色制度 0805 起已廢止，不再標色。
- 141A 為 master，動 141E 前先把 141A「請款單打法說明」整欄同步過去。
- COM 設 B4 公式：項目、廠商要**分開替換**成 `"&D4&"` 和 `"&E4&"`，整段「項目(廠商)」一起換會多一個 `"` 導致 `0x800A03EC`；設前先 kill Excel 程序。

## 打完自檢（四項，任一不過就回報）
- ① F 欄期數連續，無跳號無重號
- ② 每列 B memo 顯示的扣款/檢附＝該列 J/I 側欄值
- ③ 每列金額 G＝對應請款單
- ④ 該分頁 C4 單號已在總表登錄（沒登＝backfill 補登）

## 回測測項
- T1 新分頁的 A/B/H 是不是公式、C~K 是不是 @ 文字？
- T2 G4/J4 有無逗號、扣款小數有無被吃掉？
- T3 新分頁 tab 是否在範本(6) 右邊第一個？總表有無對應列＋超連結？
- T4 memo 有無「本期項目」「無扣款省略行並遞補編號」「工程項目不放單價行」三條？
- T5 全分頁字體是否標楷體（含超連結格）？

相關：[[feedback_self_audit_no_crutch]]、[[reference_billing_memo_standard_template]]、[[feedback_self_vs_deduct_contract]]、[[feedback_status_sync_five_places]]、[[reference_change_order_template]]、[[feedback_memory_manual_format_0805]]
