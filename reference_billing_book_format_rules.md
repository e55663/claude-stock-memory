---
name: reference_billing_book_format_rules
description: 🔴計價本格式鐵則(我多次違反被罵):memo逐字一模一樣/B4A4H4必公式G4文字含逗號/期數最舊在上/新分頁放範本旁最前
metadata: 
  node_type: memory
  type: reference
  originSessionId: 8a007757-43c3-4bd8-8838-2d576d43a25a
---

🔴 動計價本前**先讀這條**，我已因違反被使用者連續糾正、浪費他時間。打計價本鐵則：

1. **使用者給的 memo 逐字一模一樣**進 Excel，不改寫、不重排、不精簡。

2. **B4(呈核memo)、A4(資料夾名)、H4(含稅) 一律設「公式」，絕不打死純文字**。B4 公式把動態欄連回格子：單號=&C4、項目=&D4、廠商=&E4、期數=&F4、金額=&G4，其餘文字 literal 寫在公式字串裡(換行用 &CHAR(10)&)。**G4、J4 是「文字含逗號」**(例 "147,000"，非數字)。H4=`=ROUND(SUBSTITUTE(G4,",","")*1.05,0)`(不認稅/含稅項用 `=SUBSTITUTE(G4,",","")*1`)。範例見既有分頁(台灣水泥/銘亮/瑞興)。

3. **期數排列**：同廠商同 SC＝同一件事，依期數排；新期「往下新增」接在舊期那列**下面**、**不覆蓋**舊期。**最上排(row4)＝數字最小/最舊期數，越往下越新**。

4. **分頁 tab 順序**：最新的計價分頁放在「**範本(6) 旁邊＝最前面**」，越右邊越舊。新增分頁要**插在範本後面**，不是丟到最後一個(最後＝最舊)。

5. **COM 設 B4 公式雷**：項目、廠商要**分開替換**成 `"&D4&"` 和 `"&E4&"`，**不要把整段「項目(廠商)」一起換**成 `"&D4&"("&E4&")"`(尾巴多一個 `"` 會把引號關掉→公式語法錯 `0x800A03EC`)。設前先 Excel 程序 kill 乾淨避免卡 COM。

6. **(6/18 使用者新規則)** memo 第1點「本期」後面**要帶計價日期區間**，格式 `1. 本期(114/11/01~11/30)...計價金額...元(未稅)`(範本＝141A租工(銘亮)#9 那段)；區間從列控表「備註日期」抓、不掰。

7. **(6/18)署名一律要帶日期** `職 羅慶人 敬呈 115.MM.DD`(日期=問當天)。**只有追溯不到舊期呈核日才可空(例:保全#2#3補登)**，其餘一定帶。

延續 [[reference_billing_statement_template]]、[[reference_batch_memo_filing]]。對應 [[feedback_flag_problems_with_source]]、絕不掰。
