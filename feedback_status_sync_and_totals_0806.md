---
name: feedback_status_sync_and_totals_0806
description: 狀態同步與合計列手冊(整合5檔):一件狀態改要同步四處/改memo同步整列/搬核定看單號層級/加列別蓋合計列/備註只能四狀態/資料夾是真相
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 365bbf1a-11d3-4cf1-8927-79cb45fcc87f
  modified: 2026-08-06T02:42:22.024Z
---

# 狀態同步與合計列

## 一、一件狀態改 → 一次同步四處（缺一不可）
1. 計價本 **M 欄註記**
2. 計價本**總表**該列備註
3. **M月計價修改追蹤報表**該列備註
4. **Downloads 整理夾夾名後綴 ＋ 夾內 txt**（問題件加 🔴 待補/暫緩/退件+原因，解決才拿掉）

- 🔴 **tab 標色制度 115.08.05 已廢止**：不再標橘(暫緩)/紅(退件)/清色(放行)，也不用儲存格底色標狀態（表格版面用色如標頭底色不算）。既有 22 張分頁已一次清完，之後不補標也不用再清。`_計價本回測.ps1` T12 已反轉成「分頁 tab 有顏色就 FAIL」。
- 病根＝我常只改計價本或只改一部分。改完狀態自唸一次「這四處都同步了嗎」。

## 一之二、開審前先查「既有落地狀態」四查（不能只看整理夾裡的檔案）
任何案子開審——**尤其「補寫查核記錄」這種看似只是補文件的動作**——下筆前固定先跑四查：
1. **計價本該廠商分頁**：有沒有這一期、C 欄單號、M 欄註記
2. **總表**：單號登錄了沒
3. **當月追蹤報表**：有沒有那一列、承辦人是誰
4. **Downloads 各母夾 ＋ 工地歸檔**：有沒有同案舊夾（帶 🔴 的優先讀，缺件說明.txt 是前次心血）

🔴 查核記錄裡寫「尚未建／未登錄／未見」之前，一定要是「**我剛查過**」而不是「夾內沒看到」。曾在大象舉臂車#1 只讀整理夾就寫下「合約號未見於任何文件、計價本尚未建分頁」，實際上單號 141A-SC011839 就在計價本 C4／總表 R44／追蹤報表 R20／舊夾缺件說明.txt 四個地方，整案 20 天前就全部建好了。寫錯的代價是下一輪或使用者以為東西沒建，去重複建列。

## 二、改 memo ＝ 同步整列，不是只改文字
- memo 內任何數字有變 → 立刻掃**同一列所有欄位**，哪些欄承載同一個數字就一起改（台泥#2 只改了 B5 抽磅效益 0.77→0.91，同列 W5/X5/Y5/AA5 全留舊值）。
- 收尾前自問：「這個數字在這一列出現幾次？」
- ⚠️ **每個分頁的欄位配置不一定相同**：台泥分頁有 27 欄，日期在 **AA 欄**、扣款在 **Z 欄**，不是一般分頁的 K/J。用固定欄位假設去體檢會誤報「日期空白」。**動陌生分頁前先讀第 3 列標題確認欄位。**

## 三、總表搬進「核定計價」的判準＝單號層級
- 核定區的意思是「**這個單號目前沒有任何一件在跑**」。只要還有任一期在四個母夾或 Downloads 根目錄（含尚未開夾的散檔）就留在待核。
- 🔴 已在核定區的單號，日後再進新一期要把整列**搬回待核**。
- 🔴 自檢：搬區前後**總筆數必須相等**。

## 四、加列與合計列
- 🔴 **合計列的 A 欄(項次)是空白的**，用 `A欄 End(xlUp)` 找最後一列會跳過它，`last+1` 正好寫在合計列上把它覆蓋掉。
- 正確做法：加列前先用 **C 欄比對 `^計\s*\d+\s*件`** 找出合計列，新列插在合計列**之前**（或先記下、寫完重建）。
- 🔴 **合計列會過時**：改了備註沒回頭改合計列，拿去做期限盤點會歪掉（141A 原寫「送出待歸檔 15／採購議價 1」，逐列實數是 18／0）。每次動完備註，合計列一律**用程式逐列重數**（已歸檔／送出待歸檔／採購議價／退件，其餘計未定）重寫，不手動加減也不信舊值。請款單分頁字串不含「未定」、修改單分頁含「未定」，沿用該分頁原格式。
- 改共用報表（追蹤報表／計價本總表）後**一定跑 `_追蹤報表回測.ps1` 到 FAIL 0**。

## 五、備註欄只能四狀態
`送出待歸檔`／`採購議價`／`退件`／`已歸檔`，**或留空白**（＝還沒歸位）。填「暫緩」「可送呈」「已送簽核」一律 FAIL。計價本 M 欄可以寫暫緩，**追蹤報表備註不行**，該留空白。

## 六、資料夾是真相
- 每次批次歸檔後**不要只改剛搬的那幾件**，要把整張表對照五個桶重掃一次，用資料夾位置覆蓋報表舊值。
- 桶名對照：`工地\<案>\2.請款單` ＝已歸檔／`0.5 採購議價`／`1.送出待簽核完成歸檔`／`2.退件`／`3.明天整理`與根目錄＝留空白／`4.核准`（使用者核准後暫存、等我歸檔）。
- 歸檔不是終點：曾有安達#1、閎順#1 報表寫「已歸檔」但資料夾其實躺在 `2.退件`＝歸檔後又被退回來，報表沒跟著改。

## 七、腳本自己的雷（會造成假 FAIL 或真破壞）
- `N($a)*N($b)` 這種寫法 PowerShell 會把運算子吃進第一個函式的參數 → 算出垃圾值害自檢誤報。**先落地成變數再運算**。
- COM 設 `Value2` 要顯式轉型（`[string]$x.no` / `[double]$x.amt`），`-f` 產生的字串也要 `[string]`。
- 🔴 回測自己報 FAIL 時，**先懷疑是不是檢查腳本寫錯**，別急著去改本來正確的資料。

## 回測測項
- T1 這件的狀態，四處是否一致（M欄／總表／追蹤報表／夾名+txt）？
- T2 分頁 tab 有沒有殘留顏色？（有＝FAIL）
- T3 memo 數字改了，同列承載同一數字的欄位有沒有跟著改？
- T4 加列後合計列還在嗎？件數＝資料列數、金額＝逐列加總嗎？
- T5 追蹤報表備註是不是只有四狀態或空白？
- T6 報表狀態跟資料夾實際位置一致嗎（尤其歸檔後又被退回的）？

相關：[[reference_archive_workflow_0806]]、[[reference_monthly_tracking_report]]、[[reference_billing_book_summary_layout_0803]]、[[feedback_backtest_discipline]]、[[reference_excel_ps_traps_0806]]
