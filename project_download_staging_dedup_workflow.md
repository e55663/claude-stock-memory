---
name: project_download_staging_dedup_workflow
description: 🔴使用者處理請款資料的完整流程邏輯:收到資料→整理成夾→沒問題歸檔/有問題退件滯留→下次同一案再收到資料又整理成新夾但舊夾沒刪→造成重複;要主動交叉比對清掉舊夾留新夾
metadata: 
  node_type: memory
  type: project
  originSessionId: ab206e15-6560-4b96-b8b5-6d323a3cdbbd
  modified: 2026-07-23T02:46:00.905Z
---

🔴(2026/07/20)使用者說明他處理請款資料的完整邏輯，這是持續會發生的模式，要記住：

1. 使用者收到工地/廠商給的資料，丟給我，叫我整理成資料夾（依請款命名規範建夾+搬檔）。
2. 我核對後：**沒問題**的，使用者會叫我「歸檔」搬進 `工地\<案場>\2.請款單\<月份>\`；**有問題**的，會退件，資料夾就滯留在 Downloads（可能被使用者手動歸進 `-----明天整理`、`---------退件`、`-----------待歸檔` 這幾個他自己建的「待處理」母夾裡）。
3. 🔴🔴**重複的根因**：同一個案子（同廠商同期數）如果後來又收到新資料（補件/更正重送），使用者會**再丟一次給我整理成新資料夾**——但他不會主動去刪舊的那個滯留資料夾，導致 Downloads 裡同一案子會有新舊兩個資料夾並存。
4. 這造成使用者困擾：資料夾一多，分不清哪個是最新版、哪個是該丟棄的舊版。
5. 🔴**清理原則**：沒問題的案子早就被歸檔搬走了，所以還留在 Downloads（含那三個「待」母夾）裡的重複，一定是「舊的退件/待補件版」vs「新的補正重送版」——要交叉比對兩者的內容（附件是否更完整、金額是否已更正、日期是否較新），**確認新版才是最終版之後，把舊版刪掉，只留新版**。

6. 🔴使用者自述「我也潔癖，所以我希望我的下載乾淨」——這不只是這次的一次性清理請求，是他對 Downloads 資料夾的一貫期待，之後看到 Downloads 亂（重複案子、可歸檔的沒歸檔、雜檔散落）可以主動提出要不要整理，不用每次都等他明講。

**Why**：使用者不會主動清理滯留資料夾，這個「新資料進來、舊資料夾留著」的模式會持續發生，不是一次性事件，之後每次遇到都要用同一套邏輯處理；加上使用者本身重視整潔，Downloads 雜亂本身就是他在意的問題，不只是功能性障礙。

**How to apply**：
1. 定期（或使用者提起時）巡查 Downloads 根目錄與 `-----明天整理`/`---------退件`/`-----------待歸檔` 這幾個母夾，用**同廠商+同期數+同合約號**當比對鍵找出重複案子。
2. 找到重複時，比對哪個是新版（通常檔案時間較新、內容較完整、金額已修正），確認後刪除舊版，只保留新版。刪除前依 [[feedback_move_into_dir_verify_exists]] 精神先確認清楚，這是刪除操作要謹慎但使用者已經明確授權「清掉舊的留新的」這個邏輯本身，不用每次重新問。
3. 刪除的是「我自己或使用者建的整理夾」，不是工地原始來源檔案本身的唯一副本（工地給的原始檔仍在舊夾裡，刪除前留意舊夾裡是否有新夾沒有的原始檔案，該保留的先搬過去再刪夾）。

🔴🔴(2026/07/20使用者定調「這個也要回測」)**收到「歸檔」指令時，這件事本身就要固定包含「查重複+刪舊留新」這一步，不是只搬那一個資料夾就結束**：
- 歸檔前先用同廠商+同期數+同合約號搜尋 Downloads 根目錄+三個「待」母夾，看有沒有這個案子的其他舊版資料夾（例如舊的🔴問題標註夾、舊的退件夾）。
- 有找到舊版 → 確認要歸檔的這份是最終定案版之後，把舊版資料夾刪掉，只讓最終這一份進工地歸檔結構，Downloads 不留重複殘骸。
- 這一步併入 [[feedback_verify_after_batch_ops]] 的「回測」範疇——歸檔動作本身除了驗證「搬檔案有沒有搬成功」，也要驗證「這個案子在 Downloads 有沒有只剩一份、沒有重複」才算做完。

🔴🔴(2026/07/23使用者定|查重前移到「進件建夾」時、且做成機制)使用者原話「退件基本上都會再進件,那就要去核對」+「我要一個核對的機制」。**不是等歸檔才查重,收到進件建好整理夾的當下就要查**(因為退件補件重送=幾乎必然)。
- 工具:桌面 **`_請款進件查重.ps1`**(115.07.23建置,UTF8 BOM)。用法 `.\_請款進件查重.ps1`(掃 Downloads 根目錄所有「…請款單 115.MM」整理夾)或 `-Key "營建廢棄物清運(弘隆)#12"`(單查)。以**『項目(廠商)#期數』指紋**比對 `Downloads\2.退件`+`1.送出待歸檔`+`0.5 採購議價`+`3.明天整理`+`工地\*` 歸檔,列出同件舊夾。**只報告不自動刪**。
- 刪舊前三步:①核對新夾檔案已涵蓋舊夾 ②🔴舊夾的『查核記錄.txt/缺件說明.txt/三聯單等佐證』先搬進新夾保留(前次審核心血,別跟著舊夾一起刪掉) ③`Remove-Item -LiteralPath` 刪舊夾。
- 已寫進兩本計價本『請款單打法說明』(141A R121/141E R128)。
- 踩雷案例:7/23弘隆#12,舊退件夾(退件原因『三聯單不齊』)沒刪就建新夾並存被使用者抓;舊夾查核記錄.txt有**代辦256,200分攤25家完整明細**+**『佶福已達扣款上限6%本期扣0=正常非漏扣』**=寶,先搬進新夾才刪舊。詳見 [[project_billing_batch_state_0723]]。
