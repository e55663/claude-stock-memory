---
name: reference_model_choice_work_vs_chore
description: "🔴請款核對/計價/修改單這種高風險抓錯工作用Opus,雜事(搬檔/跑腳本)用Sonnet;7/20實測Sonnet漏一堆被使用者連環抓"
metadata: 
  node_type: memory
  type: reference
  originSessionId: ab206e15-6560-4b96-b8b5-6d323a3cdbbd
  modified: 2026-08-05T03:13:03.965Z
---

🔴(2026/07/20 使用者親身觀察+結論)模型選擇:
- **處理工作(請款核對、計價、修改單、審核抓錯)→用Opus**(目前Opus 4.8)。這類工作「漏一個小地方=害使用者送錯呈核文件」,懲罰很重。
- **雜事(搬檔、改字、跑固定腳本、選股撈資料)→Sonnet就夠**,便宜快。

**為什麼(額度觀察)**:使用者發現今天用Sonnet額度燒很快,以為是Sonnet單價貴。真相=**Sonnet在抓細節任務上出錯多,每次「使用者抓到→我重做→回測」都是額外好幾輪對話,token被乘上去**,不是單價問題。Opus一次做對機率高,對高風險核對工作**總額度反而更省**(省下修正回合)。7/20整天Sonnet漏了:R64整批漏建計價本、行通表沒放資料夾、問題txt沒開、字體沒統一、重複資料夾沒清、扣款memo格式連環錯——全被使用者一項項抓,浪費他大量時間。換Opus後才穩下來。

**How to apply**:使用者這邊工作類請款/計價一律建議用Opus;他成本敏感,但這是「省時間+避免送錯」的ROI,不是亂花。使用者7/20已把預設切成Opus 4.8。

🆕🔴(2026/08/04)**誰能切模型 —— 分四層，權限不同**：
1. **執行中視窗的模型＝只有使用者能換**（`/model`）。Claude 無法把自己從 Opus 切成 Sonnet，session 綁在啟動時選的模型。
2. **預設模型＝Claude 能改**（`C:\Users\Seal_Lo\.claude\settings.json` 的 `"model"`，目前 `opus`），改完下次開視窗生效。🔴不得未經使用者同意就改，等於偷偷降級他所有工作。
3. **子代理模型＝Claude 能指定**（Agent 工具的 `model` 參數，可選 sonnet/opus/haiku/fable）。這是最有用的一層：主線留 Opus 做判斷，機械性工作（歸檔搬檔／批次改名／跑既有回測腳本／Excel 抄寫）丟 Sonnet 子代理，使用者不用切來切去。🔴依使用者規則，沒他開口不主動叫子代理。
4. **Codex 模型＝Claude 能改**（`~\.codex\config.toml`，目前無 `model` 行＝跑預設 `gpt-5.6-sol`，0804 E2E 標頭確認）。

🔴**切模型的隱藏成本**：prompt cache 是各模型獨立的。同一視窗中途 `/model` 切換，前面累積的 context 要在新模型重新處理一遍，反而更花額度。規則＝**開新視窗時就決定用哪個，不要做到一半切**。

🆕🔴(2026/08/04)**7/20 的 Sonnet 結論不可直接套到 Sonnet 5**：當時測的是舊版 Sonnet。Sonnet 5 已在 coding／agentic 接近 Opus 品質，且支援 `xhigh` 推理深度（舊版沒有）；單價約 Opus 5 的六成（$3/$15 vs $5/$25 每百萬 token），導入期到 2026-08-31 為 $2/$10＝四成。拿七月舊模型的實測套八月新模型＝違反「驗證結果不得跨情境借用」。要換之前先跑對照組：拿一件已審完、答案確認過的案子（如 0803 安達#1）讓 Sonnet 5 從頭跑，用現成 45 測項回測比對漏哪幾項。
🔴**回測補不了的那塊仍歸 Opus**：既有四支回測只驗通則，不驗本輪具體宣稱；memo 定版、規則沉澱、新測項設計、行通表門檻與核決層級判定、掃描件手寫判讀、選股判盤，回測驗不到。

🆕🔴(2026/08/04 當天稍晚)**使用者已用 `/model` 把預設切成 Sonnet 5**（`settings.json` 的 `model` 欄位已從 `opus` 變 `sonnet`，下次開視窗生效）。⚠️**這不等於對照組已經跑過**——原訂的 Sonnet 5 對照試（用安達#1、比對 Codex 已驗證過的132/133人次核對結果）到收尾時都還沒開跑，只有 Opus 自己覆核過 Codex 的發現（見 [[project_billing_anda1_codex_compare_0804]]）。下次接手若要用 Sonnet 5 處理高風險請款核對，**先確認有沒有實際做過對照試，不要假設「切了預設＝已驗證過」**。

🆕🔴(2026-08-05)**對照試已中止，測試場已刪除，至今沒有任何有效分數**：
- 使用者 08-05 決定不重測，指示刪除測試資料。`Downloads\_模型測試場\` 已整個刪除（刪前逐檔 SHA256 比對，19 份案件檔全與 `1.送出待簽核完成歸檔` 原始夾一致，非孤本；原始案夾完好，安達1＝13檔、平安15＝10檔）。評分表從頭到尾空白、三邊都沒存過報告。
- 🔴**安達#1 與 平安#15 已不能再當測試場**：答案寫在 MEMORY.md 索引行（安達＝132/133命中、5/1姓名錯置、24人次跨距不足；平安＝少632,772、派工疑重複698、扣墊款63,604），而 MEMORY.md 每個 session 開場強制自動載入，**新視窗一開就被污染**，不是主動讀取才算。要重測必須換全新案子（答案卡完整、但 MEMORY.md 索引行沒寫細節），且三邊時間完全錯開、每邊跑完先存 `報告-<模型名>.txt` 再跑下一邊。
- 08-05 那輪 Sonnet 5 在已知答案下重跑安達#1（非盲測）的觀察，僅供參考不可當分數：格式守住、出處齊全、缺件誠實寫「未取得」、沒掰數字，另抓到三項記憶未載的新發現（`不會辦採購議價原因.pdf` 內容其實是鉦昇 SC011702 的退件通知放錯夾、列控表發票號碼欄空白＝整案查無發票、行通表查安達/外牆人力支援/人力支援全 0 筆）；但 33 天門禁只抽查 4 天、「24人次」直接引用記憶未自行重算，同一份資料選了比 Opus（當初寫程式全跑 133 人次）省事的做法。
- 🔴 現有證據撐不住「Opus 一定比較穩」：0803 那次閘門設太鬆（只比人數不比姓名）的結論，按 7/20 起預設就是 Opus 4.8 推算，很可能正是 Opus 跑的；而唯一一次從零跑完整案且全中的是 Codex（非 Claude）。分層結論仍成立（送得出門的用最穩的、雜事丟 Sonnet 子代理），但那是罰則不對稱的推論，不是實測排名。

🆕(2026/07/27 查證)**Opus 5 已於2026/07/23~24發布**(此事在AI知識截止2026/01之後,新session會不知道、要主動查證別硬說沒有):1M脈絡+xhigh推理+每回合切low/med/high控成本+安全回退4.8;價格$5/$25每百萬token=與4.8同價、不變貴;coding/知識工作評測SOTA(逼近Fable 5半價定位)。→工作類請款/計價改用Opus 5(同價更強、還能用low省雜事)。🔴若`/model`看不到Opus 5:模型清單編進CLI build+running session記在記憶體→(a)CLI需≥2.1.219(使用者115.07.27實測已是2.1.220、版本夠),(b)真正原因=此視窗在更新前開的,關掉重開新session即出現,或`/model`手動打型號ID `claude-opus-5`。
